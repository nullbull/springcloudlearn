spring:
  application:
    name: niu-auth
  cloud:
    config:
      profile: dev
      name: application
      discovery:
        service-id: niu-config
        enabled: true
    bus:
      trace:
        enabled: true
  rabbitmq:
    username: root
    password: root
    virtual-host: /zwt
    host: localhost
    port: 5672
server:
  port: 8091
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
