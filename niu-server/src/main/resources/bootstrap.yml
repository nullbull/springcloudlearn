spring:
  application:
    name: niu-server
  cloud:
    config:
      profile: dev
      name: application
      discovery:
        service-id: niu-config
        enabled: true
    bus:
      trace:
        enabled: true
  rabbitmq:
    username: root
    password: root
    virtual-host: /zwt
    host: localhost
    port: 5672
server:
  port: 8088
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
management:
  security:
    enabled: false
